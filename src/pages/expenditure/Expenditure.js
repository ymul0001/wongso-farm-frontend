import React from 'react';
import Header from '../../components/header/Header';
import './expenditure.css';

const Expenditure = () => {
    return(
        <div className="expenditure">
            <Header title="Expenditure"/>
            <h1>This is Expenditure Page</h1>
        </div>
    )
}

export default Expenditure;